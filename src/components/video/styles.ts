import {
  Theme,
} from 'elephanz-rn-ui';
import {
  VideoComponentStyles,
} from './types';

export const styles = (theme: Theme): VideoComponentStyles => ({
  container: {
    flex: 1,
  },
  player: {
    flex: 1,
  },
});
