import {
  Theme,
} from 'elephanz-rn-ui';
import {
  HeaderComponentStyles,
} from './types';

export const styles = (theme: Theme): HeaderComponentStyles => ({
  container: {
    flex: 1,
  },
  imageBackground: {
    flex: 1,
  },
  linearGradient: {
    flex: 1,
  },
});
