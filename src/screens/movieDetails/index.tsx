import React from 'react';
import {
  View,
} from 'react-native';
import {
  baseScreen,
} from 'hoc';

const movieDetailsScreen: React.FC = () => {
  return <View />;
}

export const MovieDetailsScreen = baseScreen(
  movieDetailsScreen,
);
