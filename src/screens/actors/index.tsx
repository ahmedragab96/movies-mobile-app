import React from 'react';
import {
  View,
} from 'react-native';
import {
  baseScreen,
} from 'hoc';

const actorsScreen: React.FC = () => {
  return <View />;
}

export const ActorsScreen = baseScreen(
  actorsScreen,
);
