import React from 'react';
import {
  View,
} from 'react-native';
import {
  baseScreen,
} from 'hoc';

const genreDetailsScreen: React.FC = () => {
  return <View />;
}

export const GenreDetailsScreen = baseScreen(
  genreDetailsScreen,
);
