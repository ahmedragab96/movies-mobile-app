import React from 'react';
import {
  View,
} from 'react-native';
import {
  baseScreen,
} from 'hoc';

const actorDetailsScreen: React.FC = () => {
  return <View />;
}

export const ActorDetailsScreen = baseScreen(
  actorDetailsScreen,
);
